{
  "version": 1,
  "findings": {
    "apps/api/src/modules/admin-access/admin-access.service.ts#createInvitation#adminAuditLog.create": {
      "status": "confirmed",
      "reason": "Admin invitation creation and verification token persistence commit inside one transaction, but the CREATE_ADMIN_INVITATION audit write currently executes after that transaction. An audit failure can therefore leave a durable invitation without its required audit record."
    }
  }
}
