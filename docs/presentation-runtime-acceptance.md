# Presentation runtime acceptance

The implementation is accepted when CI confirms:

- API returns shared/platform game and provider presentation URLs.
- Admin can edit shared, PC, and Mobile presentation values.
- Member runtime shows configured data first and presentation fallback only when needed.
- Tournament, leaderboard, promotions, activities, mini games, and FAQ sections are populated for presentation mode.
- Featured games are selected through existing Game Control flags.
