'use client';

import { FormEvent, useEffect, useMemo, useState } from 'react';
import { adminApiFetch } from '../../admin-api';
import { AdminBadge, AdminButton, AdminCard, AdminEmpty, AdminMetric, AdminMetricGrid, AdminNotice, AdminPage, AdminRow, AdminStack, AdminToolbar } from '../_components/admin-ui';

type GameStatus = 'ACTIVE' | 'INACTIVE' | 'MAINTENANCE' | 'REMOVED';
type MediaType = 'COVER' | 'ICON' | 'THUMBNAIL' | 'BANNER' | 'LOGO' | 'FALLBACK';
type Provider = { id: string; name: string; code: string };
type GameMedia = { id: string; type: MediaType; sourceUrl?: string | null; cachedUrl?: string | null; status: string; isOverride: boolean };
type Game = { id: string; providerId: string; providerGameCode: string; name: string; category: string; status: GameStatus; sortOrder: number; isFeatured: boolean; isNew: boolean; isPopular: boolean; provider?: Provider; media?: GameMedia[]; updatedAt: string };
type GameForm = { id?: string; providerId: string; providerGameCode: string; name: string; category: string; status: GameStatus; sortOrder: string; isFeatured: boolean; isNew: boolean; isPopular: boolean };
type MediaForm = { gameId: string; type: MediaType; sourceUrl: string };

const emptyForm: GameForm = { providerId: '', providerGameCode: '', name: '', category: 'slot', status: 'INACTIVE', sortOrder: '100', isFeatured: false, isNew: false, isPopular: false };
const emptyMediaForm: MediaForm = { gameId: '', type: 'COVER', sourceUrl: '' };
const statuses: GameStatus[] = ['ACTIVE', 'INACTIVE', 'MAINTENANCE', 'REMOVED'];
const mediaTypes: MediaType[] = ['COVER', 'ICON', 'THUMBNAIL', 'BANNER', 'LOGO', 'FALLBACK'];

export default function GameCatalogPage() {
  const [games, setGames] = useState<Game[]>([]); const [providers, setProviders] = useState<Provider[]>([]); const [form, setForm] = useState<GameForm>(emptyForm); const [mediaForm, setMediaForm] = useState<MediaForm>(emptyMediaForm);
  const [message, setMessage] = useState(''); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false);
  useEffect(() => { loadAll(); }, []);
  const metrics = useMemo(() => ({ total: games.length, active: games.filter((item) => item.status === 'ACTIVE').length, featured: games.filter((item) => item.isFeatured).length, popular: games.filter((item) => item.isPopular).length }), [games]);
  async function loadAll() { setLoading(true); setMessage('กำลังโหลด catalog...'); const [gamesRes, providersRes] = await Promise.all([adminApiFetch('/admin/games'), adminApiFetch('/admin/game-providers')]); const gamesData = await gamesRes.json().catch(() => null); const providersData = await providersRes.json().catch(() => null); setLoading(false); if (!gamesRes.ok) { setMessage(gamesData?.message ?? 'โหลดเกมไม่สำเร็จ'); return; } if (!providersRes.ok) { setMessage(providersData?.message ?? 'โหลด provider ไม่สำเร็จ'); return; } const providerItems = providersData.items ?? []; const gameItems = gamesData.items ?? []; setGames(gameItems); setProviders(providerItems); setForm((current) => ({ ...current, providerId: current.providerId || providerItems[0]?.id || '' })); setMediaForm((current) => ({ ...current, gameId: current.gameId || gameItems[0]?.id || '' })); setMessage(''); }
  function updateField<K extends keyof GameForm>(key: K, value: GameForm[K]) { setForm((current) => ({ ...current, [key]: value })); }
  function updateMediaField<K extends keyof MediaForm>(key: K, value: MediaForm[K]) { setMediaForm((current) => ({ ...current, [key]: value })); }
  function editGame(item: Game) { setForm({ id: item.id, providerId: item.providerId, providerGameCode: item.providerGameCode, name: item.name, category: item.category, status: item.status, sortOrder: String(item.sortOrder), isFeatured: item.isFeatured, isNew: item.isNew, isPopular: item.isPopular }); setMediaForm((current) => ({ ...current, gameId: item.id })); setMessage(`กำลังแก้ไข ${item.name}`); }
  function resetForm() { setForm({ ...emptyForm, providerId: providers[0]?.id || '' }); setMessage(''); }
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const payload = { providerId: form.providerId, providerGameCode: form.providerGameCode.trim(), name: form.name.trim(), category: form.category.trim(), status: form.status, sortOrder: Number(form.sortOrder || 100), isFeatured: form.isFeatured, isNew: form.isNew, isPopular: form.isPopular }; if (!payload.providerId || !payload.providerGameCode || !payload.name || !payload.category) { setMessage('กรุณากรอก provider, game code, name และ category'); return; } setSaving(true); const res = await adminApiFetch(form.id ? `/admin/games/${form.id}` : '/admin/games', { method: form.id ? 'PATCH' : 'POST', body: JSON.stringify(payload) }); const data = await res.json().catch(() => null); setSaving(false); if (!res.ok) { setMessage(data?.message ?? 'บันทึกเกมไม่สำเร็จ'); return; } setMessage(form.id ? 'บันทึกเกมแล้ว' : 'สร้างเกมแล้ว'); setForm({ ...emptyForm, providerId: providers[0]?.id || '' }); await loadAll(); }
  async function submitMedia(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!mediaForm.gameId || !mediaForm.sourceUrl.trim()) { setMessage('กรุณาเลือกเกมและใส่ URL รูป'); return; } const res = await adminApiFetch(`/admin/games/${mediaForm.gameId}/media`, { method: 'POST', body: JSON.stringify({ type: mediaForm.type, sourceUrl: mediaForm.sourceUrl.trim(), cachedUrl: mediaForm.sourceUrl.trim(), status: 'READY', isOverride: true }) }); const data = await res.json().catch(() => null); if (!res.ok) { setMessage(data?.message ?? 'บันทึกรูปเกมไม่สำเร็จ'); return; } setMediaForm({ ...emptyMediaForm, gameId: mediaForm.gameId }); setMessage('บันทึกรูปเกมแล้ว'); await loadAll(); }
  async function patchGame(item: Game, patch: Partial<Game>) { const res = await adminApiFetch(`/admin/games/${item.id}`, { method: 'PATCH', body: JSON.stringify(patch) }); const data = await res.json().catch(() => null); if (!res.ok) { setMessage(data?.message ?? 'อัปเดตเกมไม่สำเร็จ'); return; } setGames((current) => current.map((game) => game.id === item.id ? { ...game, ...data } : game)); setMessage('อัปเดตเกมแล้ว'); }
  return <AdminPage eyebrow="Game Platform" title="Game Catalog" description="จัดการรายชื่อเกม หมวดหมู่ สถานะ flag และรูปเกม" actions={<AdminButton onClick={loadAll} disabled={loading}>Refresh</AdminButton>}><AdminMetricGrid><AdminMetric title="Games" value={String(metrics.total)} helper="all catalog games" /><AdminMetric title="Active" value={String(metrics.active)} helper="เปิดใช้งานอยู่" /><AdminMetric title="Featured" value={String(metrics.featured)} helper="แนะนำ" /><AdminMetric title="Popular" value={String(metrics.popular)} helper="ยอดนิยม" /></AdminMetricGrid>{message && <AdminNotice>{message}</AdminNotice>}<AdminCard title={form.id ? 'Edit game' : 'Create game'} description="เพิ่มเกมเข้าคลังแบบ manual ก่อน sync จาก provider จริง"><form onSubmit={submit} style={formStyle}><label style={labelStyle}>Provider<select value={form.providerId} onChange={(event) => updateField('providerId', event.target.value)} style={inputStyle}>{providers.map((provider) => <option key={provider.id} value={provider.id}>{provider.name} ({provider.code})</option>)}</select></label><label style={labelStyle}>Provider game code<input value={form.providerGameCode} onChange={(event) => updateField('providerGameCode', event.target.value)} style={inputStyle} placeholder="demo-slot-001" /></label><label style={labelStyle}>Game name<input value={form.name} onChange={(event) => updateField('name', event.target.value)} style={inputStyle} /></label><label style={labelStyle}>Category<input value={form.category} onChange={(event) => updateField('category', event.target.value)} style={inputStyle} /></label><label style={labelStyle}>Status<select value={form.status} onChange={(event) => updateField('status', event.target.value as GameStatus)} style={inputStyle}>{statuses.map((status) => <option key={status}>{status}</option>)}</select></label><label style={labelStyle}>Sort order<input value={form.sortOrder} onChange={(event) => updateField('sortOrder', event.target.value)} inputMode="numeric" style={inputStyle} /></label><label style={checkStyle}><input type="checkbox" checked={form.isFeatured} onChange={(event) => updateField('isFeatured', event.target.checked)} /> Featured</label><label style={checkStyle}><input type="checkbox" checked={form.isNew} onChange={(event) => updateField('isNew', event.target.checked)} /> New</label><label style={checkStyle}><input type="checkbox" checked={form.isPopular} onChange={(event) => updateField('isPopular', event.target.checked)} /> Popular</label><div style={actionRowStyle}><AdminButton type="submit" disabled={saving}>{saving ? 'Saving...' : form.id ? 'Save game' : 'Create game'}</AdminButton>{form.id && <AdminButton type="button" tone="secondary" onClick={resetForm}>Cancel edit</AdminButton>}</div></form></AdminCard><AdminCard title="Game media" description="เพิ่มรูปพื้นฐานให้เกม: cover, icon, banner หรือ fallback"><form onSubmit={submitMedia} style={formStyle}><label style={labelStyle}>Game<select value={mediaForm.gameId} onChange={(event) => updateMediaField('gameId', event.target.value)} style={inputStyle}>{games.map((game) => <option key={game.id} value={game.id}>{game.name}</option>)}</select></label><label style={labelStyle}>Type<select value={mediaForm.type} onChange={(event) => updateMediaField('type', event.target.value as MediaType)} style={inputStyle}>{mediaTypes.map((type) => <option key={type}>{type}</option>)}</select></label><label style={labelStyle}>Image URL<input value={mediaForm.sourceUrl} onChange={(event) => updateMediaField('sourceUrl', event.target.value)} style={inputStyle} placeholder="https://..." /></label><div style={actionRowStyle}><AdminButton type="submit">Add media</AdminButton></div></form></AdminCard><AdminToolbar><strong>Game list</strong><span style={mutedStyle}>{loading ? 'Loading...' : `${games.length} games loaded`}</span></AdminToolbar><AdminStack>{games.map((item) => <AdminCard key={item.id}><AdminRow><div><h2 style={gameNameStyle}>{item.name}</h2><p style={mutedStyle}>{item.provider?.name ?? item.providerId} · {item.providerGameCode} · {item.category}</p><p style={smallMutedStyle}>Media {(item.media ?? []).length}</p></div><div style={badgeStackStyle}><AdminBadge tone={statusTone(item.status)}>{item.status}</AdminBadge>{item.isFeatured && <AdminBadge>FEATURED</AdminBadge>}{item.isNew && <AdminBadge>NEW</AdminBadge>}{item.isPopular && <AdminBadge>POPULAR</AdminBadge>}</div></AdminRow><div style={actionRowStyle}><AdminButton tone="secondary" onClick={() => editGame(item)}>Edit</AdminButton><AdminButton tone={item.status === 'ACTIVE' ? 'danger' : 'success'} onClick={() => patchGame(item, { status: item.status === 'ACTIVE' ? 'MAINTENANCE' : 'ACTIVE' } as Partial<Game>)}>{item.status === 'ACTIVE' ? 'Maintenance' : 'Activate'}</AdminButton><AdminButton tone="secondary" onClick={() => patchGame(item, { isFeatured: !item.isFeatured } as Partial<Game>)}>Featured</AdminButton><AdminButton tone="secondary" onClick={() => patchGame(item, { isNew: !item.isNew } as Partial<Game>)}>New</AdminButton><AdminButton tone="secondary" onClick={() => patchGame(item, { isPopular: !item.isPopular } as Partial<Game>)}>Popular</AdminButton></div><p style={smallMutedStyle}>Sort {item.sortOrder} · Updated {new Date(item.updatedAt).toLocaleString('th-TH')}</p></AdminCard>)}{!loading && games.length === 0 && <AdminEmpty>ยังไม่มีเกม เพิ่มเกมแรกจากฟอร์มด้านบน</AdminEmpty>}</AdminStack></AdminPage>;
}
function statusTone(status: GameStatus) { if (status === 'ACTIVE') return 'success'; if (status === 'MAINTENANCE') return 'warning'; if (status === 'REMOVED') return 'danger'; return 'neutral'; }
const formStyle = { display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(min(230px, 100%), 1fr))', gap: 12, minWidth: 0 } as const;
const labelStyle = { display: 'grid', gap: 6, color: '#cbd5e1', fontWeight: 900, minWidth: 0 } as const;
const inputStyle = { width: '100%', minHeight: 44, borderRadius: 12, border: '1px solid rgba(148,163,184,.22)', background: '#0b1220', color: '#f8fafc', padding: '0 12px', boxSizing: 'border-box' as const, fontSize: 15 };
const checkStyle = { display: 'flex', gap: 8, alignItems: 'center', color: '#cbd5e1', fontWeight: 900 } as const;
const actionRowStyle = { display: 'flex', gap: 10, flexWrap: 'wrap' as const, alignItems: 'center' } as const;
const mutedStyle = { margin: 0, color: '#94a3b8', lineHeight: 1.55 } as const;
const smallMutedStyle = { margin: 0, color: '#64748b', fontSize: 12 } as const;
const badgeStackStyle = { display: 'flex', gap: 8, flexWrap: 'wrap' as const, justifyContent: 'flex-end' as const };
const gameNameStyle = { margin: 0, fontSize: 22, lineHeight: 1.12 } as const;
