name: R-006 Quality Baseline

on:
  pull_request:
  push:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/platform_ci?schema=public
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile=false
      - name: Detect changed scopes
        id: scope
        env:
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
        run: pnpm ci:changed-scope
      - name: Prisma schema and generated-client drift
        run: |
          pnpm exec prisma validate --schema prisma/schema.prisma
          pnpm audit:generated-drift
      - name: Migration validation
        run: pnpm audit:migration-validation
      - name: Architecture and test-safety guards
        run: |
          pnpm audit:architecture-inventory
          pnpm audit:architecture-boundaries
          pnpm audit:circular-dependencies
          pnpm audit:critical-test-safety
          pnpm audit:browser-quality
          pnpm audit:r1-closure
          pnpm audit:r2-closure
          pnpm audit:r3-closure
      - name: R-009 controller persistence boundary
        run: |
          pnpm audit:r9-controller-prisma
          pnpm audit:r9-controller-prisma:json > r009-controller-prisma-inventory.json
          pnpm audit:r9-controller-prisma:strict
          node tools/audit-r009-controller-closure.mjs
      - name: R-009 transaction lock-order boundary
        run: |
          pnpm audit:r9-lock-order
          pnpm audit:r9-lock-order:json > r009-lock-order-inventory.json
          pnpm audit:r9-lock-order:strict
      - name: R-009 repository type boundary
        run: |
          pnpm audit:r9-repository-boundaries
          pnpm audit:r9-repository-boundaries:json > r009-repository-boundaries.json
          pnpm audit:r9-repository-boundaries:strict
      - name: R-009 boundary closure structure
        run: pnpm audit:r9-boundary-closure
      - name: R-009 transaction escape inventory
        run: |
          pnpm audit:r9-transaction-escapes
          pnpm audit:r9-transaction-escapes:json > r009-transaction-escapes.json
      - name: R-009 schema constraint inventory
        run: |
          pnpm audit:r9-schema-constraints
          pnpm audit:r9-schema-constraints:json > r009-schema-constraints.json
      - name: Upload R-009 inventories
        uses: actions/upload-artifact@v4
        with:
          name: r009-persistence-inventories-${{ github.run_id }}
          path: |
            r009-controller-prisma-inventory.json
            r009-lock-order-inventory.json
            r009-repository-boundaries.json
            r009-transaction-escapes.json
            r009-schema-constraints.json
          if-no-files-found: error
          retention-days: 30
      - name: Lint API
        if: steps.scope.outputs.api == 'true'
        run: pnpm lint:api
      - name: Lint Admin
        if: steps.scope.outputs.admin == 'true'
        run: pnpm lint:admin
      - name: Lint Member
        if: steps.scope.outputs.member == 'true'
        run: pnpm lint:member
      - name: Lint packages
        if: steps.scope.outputs.packages == 'true'
        run: pnpm lint:packages
      - name: Typecheck API
        if: steps.scope.outputs.api == 'true'
        run: pnpm typecheck:api
      - name: Typecheck Admin
        if: steps.scope.outputs.admin == 'true'
        run: pnpm typecheck:admin
      - name: Typecheck Member
        if: steps.scope.outputs.member == 'true'
        run: pnpm typecheck:member
      - name: Typecheck packages
        if: steps.scope.outputs.packages == 'true'
        run: pnpm typecheck:packages
      - name: Package export audit
        run: pnpm audit:unused-exports
      - name: R-006 closure structure audit
        run: pnpm audit:r6-closure
      - name: Quality summary
        if: always()
        run: |
          {
            echo '## R-001–R-006 Quality Baseline'
            echo "- API scope: ${{ steps.scope.outputs.api || 'unknown' }}"
            echo "- Admin scope: ${{ steps.scope.outputs.admin || 'unknown' }}"
            echo "- Member scope: ${{ steps.scope.outputs.member || 'unknown' }}"
            echo "- Packages scope: ${{ steps.scope.outputs.packages || 'unknown' }}"
            echo '- R-001 architecture inventory, R-002 boundaries, R-003 regression safety and R-006 quality closure gates execute unconditionally'
            echo '- R-009 controller, repository type and lock-order boundaries are strict and protected by closure audits'
            echo '- R-009 transaction-escape and schema inventories remain review-only'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload quality failure evidence
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: r006-quality-failure-${{ github.run_id }}
          if-no-files-found: ignore
          retention-days: 14
          path: |
            playwright-report/
            test-results/
            coverage/
            npm-debug.log
            pnpm-debug.log
