name: Admin Icons Endpoint Verify

on:
  push:
    branches:
      - fix/admin-icons-settings-endpoint
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: admin-icons-endpoint-verify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/admin-icons-settings-endpoint
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Register icons in service and Prisma schema
        run: |
          python <<'PY'
          from pathlib import Path

          service = Path('apps/api/src/modules/settings/settings.service.ts')
          source = service.read_text()
          old = "const publicGroups: SettingGroupSlug[] = ['website', 'branding', 'theme', 'seo', 'contact', 'maintenance', 'features', 'legal'];"
          new = "const publicGroups: SettingGroupSlug[] = ['website', 'branding', 'icons', 'theme', 'seo', 'contact', 'maintenance', 'features', 'legal'];"
          if old not in source and new not in source:
              raise SystemExit('Expected public settings group line was not found')
          service.write_text(source.replace(old, new))

          schema = Path('prisma/schema.prisma')
          schema_source = schema.read_text()
          marker = 'enum SiteSettingGroup {\n  WEBSITE\n  BRANDING\n'
          replacement = 'enum SiteSettingGroup {\n  WEBSITE\n  BRANDING\n  ICONS\n'
          if marker not in schema_source and replacement not in schema_source:
              raise SystemExit('SiteSettingGroup enum marker was not found')
          schema.write_text(schema_source.replace(marker, replacement))
          PY
      - run: pnpm install --frozen-lockfile=false
      - name: Validate Prisma schema
        run: pnpm exec prisma validate --schema prisma/schema.prisma
      - name: Generate Prisma client
        run: pnpm exec prisma generate --schema prisma/schema.prisma
      - name: API typecheck
        run: pnpm --filter @platform/api typecheck
      - name: Icon endpoint tests
        run: pnpm --filter @platform/api test -- settings-icons.spec.ts --runInBand
      - name: API production build
        run: pnpm --filter @platform/api build
      - name: Commit validated integration
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add apps/api/src/modules/settings/settings.service.ts prisma/schema.prisma
          if git diff --cached --quiet; then
            echo 'Validated integration is already committed.'
            exit 0
          fi
          git commit -m 'fix(settings): publish dedicated icons settings group'
          git push origin HEAD:fix/admin-icons-settings-endpoint
