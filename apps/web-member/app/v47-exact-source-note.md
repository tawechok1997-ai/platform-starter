# V47 exact source lock

The user-provided archive is the visual source of truth. Rebuilds must preserve its DOM order, asset selection, CSS geometry, and responsive behavior before reconnecting dynamic APIs.
