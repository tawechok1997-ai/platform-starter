import type { AdminActor, MemberActor } from '../../common/actors';
import { buildAdminAuditData } from '../../common/audit/admin-audit.builder';
import { BadRequestException, ForbiddenException, Injectable, NotFoundException } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Prisma } from '@prisma/client';
import { createDecipheriv, createHash, randomBytes } from 'crypto';
import { PrismaService } from '../../database/prisma.service';
import { GameProviderEndpointType, GameProviderWalletMode } from './game-platform.types';
import { ProviderAdapterContext } from './provider-adapter.interface';
import { ProviderAdapterRegistry } from './adapters/provider-adapter.registry';

type RequestMeta = { ipAddress?: string; userAgent?: string };
type TransferKind = 'TRANSFER_IN' | 'TRANSFER_OUT';
type ProviderGates = Partial<Record<'launchEnabled' | 'transferEnabled' | 'realMoneyEnabled' | 'webhookSettlementEnabled' | 'walletSyncEnabled', boolean>>;
type ProviderWithAdapterData = { code: string; walletMode: GameProviderWalletMode; currency: string; metadata?: unknown; endpoints: Array<{ type: GameProviderEndpointType; url: string; timeoutMs: number; isEnabled?: boolean }>; credentials: Array<{ type: string; maskedValue: string; encryptedValue?: string; isEnabled?: boolean }> };
type WalletMutationInput = { userId: string; type: 'TRANSFER' | 'REVERSAL'; direction: 'CREDIT' | 'DEBIT'; amount: number; referenceType: string; referenceId: string; idempotencyKey: string; metadata: Record<string, unknown> };

@Injectable()
export class GamePlatformMoneyService {
  constructor(private readonly prisma: PrismaService, private readonly adapterRegistry: ProviderAdapterRegistry, private readonly configService: ConfigService) {}

  async transferDryRun(sessionId: string, actor: MemberActor, type: TransferKind, amount: string, meta: RequestMeta) { const session = await this.prisma.gameSession.findFirst({ where: { id: sessionId, userId: actor.id, status: { in: ['LAUNCHED', 'ACTIVE'] } }, include: { provider: { include: { endpoints: { where: { isEnabled: true }, orderBy: { type: 'asc' } }, credentials: { where: { isEnabled: true }, orderBy: { type: 'asc' }, select: this.credentialSelect() } } }, game: { select: { id: true, name: true, providerGameCode: true } } } }); if (!session) throw new NotFoundException('Game session is not available for transfer'); this.assertTransferSafetyGate(session.provider, type); return this.createWalletSyncedTransfer({ userId: actor.id, providerId: session.providerId, sessionId: session.id, type, amount, currency: session.provider.currency, provider: session.provider, requestPayload: { walletSync: true, type, sessionId: session.id, gameId: session.gameId, gameCode: session.game.providerGameCode, ipAddress: meta.ipAddress, userAgent: meta.userAgent } }); }
  async retryDryRunTransfer(id: string, actor: AdminActor, note: string) { const original = await this.prisma.gameTransfer.findUnique({ where: { id }, include: { session: { include: { game: { select: { providerGameCode: true } }, provider: { include: { endpoints: { where: { isEnabled: true }, orderBy: { type: 'asc' } }, credentials: { where: { isEnabled: true }, orderBy: { type: 'asc' }, select: this.credentialSelect() } } } } } } }); if (!original) throw new NotFoundException('Game transfer not found'); if (original.status !== 'FAILED') throw new BadRequestException('Only FAILED transfers can be retried'); if (!original.session) throw new BadRequestException('Original transfer has no session'); this.assertTransferSafetyGate(original.session.provider, original.type as TransferKind); const result = await this.createWalletSyncedTransfer({ userId: original.userId, providerId: original.providerId, sessionId: original.sessionId ?? undefined, type: original.type as TransferKind, amount: original.amount.toString(), currency: original.currency, provider: original.session.provider, requestPayload: { walletSync: true, retry: true, originalTransferId: original.id, retryBy: actor.id, retryNote: note, gameCode: original.session.game.providerGameCode } }); await this.audit(actor, 'game.transfer.retry_wallet_sync', 'game_platform', original.id, { note, retryTransferId: result.transfer.id, walletSync: result.walletSync }); return result; }
  private async createWalletSyncedTransfer(input: { userId: string; providerId: string; sessionId?: string; type: TransferKind; amount: string; currency: string; provider: ProviderWithAdapterData; requestPayload: Record<string, unknown> }) { const amount = this.parseAmount(input.amount); const idempotencyKey = `${input.type.toLowerCase()}_${input.sessionId ?? input.providerId}_${Date.now()}_${randomBytes(4).toString('hex')}`; const transfer = await this.prisma.gameTransfer.create({ data: { userId: input.userId, providerId: input.providerId, sessionId: input.sessionId, type: input.type, status: 'PENDING', amount: amount.toFixed(2), currency: input.currency, idempotencyKey, requestPayload: this.safeJson({ ...input.requestPayload, idempotencyKey }) } }); const adapter = this.adapterRegistry.getAdapter(input.provider.code); await this.markCredentialUse(input.providerId); const adapterInput = { userId: input.userId, amount: amount.toFixed(2), currency: input.currency, idempotencyKey, sessionId: input.sessionId }; if (input.type === 'TRANSFER_IN') { const debit = await this.applyWalletMutation({ userId: input.userId, type: 'TRANSFER', direction: 'DEBIT', amount, referenceType: 'GAME_TRANSFER', referenceId: transfer.id, idempotencyKey: `${idempotencyKey}_wallet_debit`, metadata: { transferId: transfer.id, providerId: input.providerId, sessionId: input.sessionId, direction: 'wallet_to_provider' } }); const result = await adapter.transferIn(this.buildAdapterContext(input.provider), adapterInput); if (result.ok && result.payload) { const updated = await this.prisma.gameTransfer.update({ where: { id: transfer.id }, data: { status: 'SUCCESS', providerTransactionId: result.payload.providerTransactionId, responsePayload: this.safeJson({ ...result, walletLedgerId: debit.ledger.id, walletBalanceAfter: debit.wallet.balance }), resolvedAt: new Date() }, include: this.transferInclude() }); return { ok: true, transfer: updated, walletSync: { direction: 'DEBIT', ledgerId: debit.ledger.id, balanceAfter: debit.wallet.balance }, realMutation: true, safetyGate: 'wallet-synced-transfer' }; } const rollback = await this.applyWalletMutation({ userId: input.userId, type: 'REVERSAL', direction: 'CREDIT', amount, referenceType: 'GAME_TRANSFER_ROLLBACK', referenceId: transfer.id, idempotencyKey: `${idempotencyKey}_wallet_rollback`, metadata: { transferId: transfer.id, providerId: input.providerId, sessionId: input.sessionId, reason: result.errorCode ?? 'PROVIDER_TRANSFER_IN_FAILED' } }); const updated = await this.prisma.gameTransfer.update({ where: { id: transfer.id }, data: { status: 'FAILED', responsePayload: this.safeJson({ ...result, walletDebitLedgerId: debit.ledger.id, walletRollbackLedgerId: rollback.ledger.id, walletBalanceAfterRollback: rollback.wallet.balance }), errorCode: result.errorCode ?? 'TRANSFER_FAILED', errorMessage: result.errorMessage ?? 'Provider transfer-in failed; wallet debit was rolled back', resolvedAt: new Date() }, include: this.transferInclude() }); return { ok: false, transfer: updated, walletSync: { debitLedgerId: debit.ledger.id, rollbackLedgerId: rollback.ledger.id, balanceAfter: rollback.wallet.balance }, realMutation: true, rolledBack: true, safetyGate: 'wallet-synced-transfer' }; } const result = await adapter.transferOut(this.buildAdapterContext(input.provider), adapterInput); if (result.ok && result.payload) { const credit = await this.applyWalletMutation({ userId: input.userId, type: 'TRANSFER', direction: 'CREDIT', amount, referenceType: 'GAME_TRANSFER', referenceId: transfer.id, idempotencyKey: `${idempotencyKey}_wallet_credit`, metadata: { transferId: transfer.id, providerId: input.providerId, sessionId: input.sessionId, providerTransactionId: result.payload.providerTransactionId, direction: 'provider_to_wallet' } }); const updated = await this.prisma.gameTransfer.update({ where: { id: transfer.id }, data: { status: 'SUCCESS', providerTransactionId: result.payload.providerTransactionId, responsePayload: this.safeJson({ ...result, walletLedgerId: credit.ledger.id, walletBalanceAfter: credit.wallet.balance }), resolvedAt: new Date() }, include: this.transferInclude() }); return { ok: true, transfer: updated, walletSync: { direction: 'CREDIT', ledgerId: credit.ledger.id, balanceAfter: credit.wallet.balance }, realMutation: true, safetyGate: 'wallet-synced-transfer' }; } const updated = await this.prisma.gameTransfer.update({ where: { id: transfer.id }, data: { status: 'FAILED', responsePayload: this.safeJson(result), errorCode: result.errorCode ?? 'TRANSFER_FAILED', errorMessage: result.errorMessage ?? 'Provider transfer-out failed; wallet was not changed', resolvedAt: new Date() }, include: this.transferInclude() }); return { ok: false, transfer: updated, walletSync: { changed: false }, realMutation: false, safetyGate: 'wallet-synced-transfer' }; }
  private async applyWalletMutation(input: WalletMutationInput) { return this.prisma.$transaction(async (tx) => { const existing = await tx.walletLedger.findUnique({ where: { idempotencyKey: input.idempotencyKey } }); if (existing) { const existingWallet = await tx.wallet.findUnique({ where: { id: existing.walletId } }); if (!existingWallet) throw new BadRequestException('Wallet not found for existing ledger'); return { ledger: existing, wallet: existingWallet }; } const wallet = await tx.wallet.findUnique({ where: { userId: input.userId } }); if (!wallet) throw new BadRequestException('Wallet not found'); if (wallet.status !== 'ACTIVE') throw new BadRequestException('Wallet is not active'); const balanceBefore = Number(wallet.balance); const balanceAfter = input.direction === 'CREDIT' ? balanceBefore + input.amount : balanceBefore - input.amount; if (balanceAfter < 0) throw new BadRequestException('ยอดคงเหลือไม่พอสำหรับโยกเงินเข้าเกม'); const updatedWallet = await tx.wallet.update({ where: { id: wallet.id }, data: { balance: balanceAfter.toFixed(2) } }); const ledger = await tx.walletLedger.create({ data: { walletId: wallet.id, userId: input.userId, type: input.type, direction: input.direction, amount: input.amount.toFixed(2), balanceBefore: balanceBefore.toFixed(2), balanceAfter: balanceAfter.toFixed(2), referenceType: input.referenceType, referenceId: input.referenceId, idempotencyKey: input.idempotencyKey, metadata: this.safeJson(input.metadata) } }); return { ledger, wallet: updatedWallet }; }); }
  async updateProviderGates(providerId: string, actor: AdminActor, gates: ProviderGates) { const provider = await this.prisma.gameProvider.findUnique({ where: { id: providerId } }); if (!provider) throw new NotFoundException('Game provider not found'); const current = this.objectJson(provider.metadata); const next = { ...current, ...gates, gateUpdatedBy: actor.id, gateUpdatedAt: new Date().toISOString() }; const item = await this.prisma.gameProvider.update({ where: { id: providerId }, data: { metadata: this.safeJson(next) }, select: { id: true, name: true, code: true, metadata: true } }); await this.audit(actor, 'game.provider.gates.update', 'game_platform', providerId, { before: current, after: item.metadata }); return { ok: true, item, gates: this.extractGates(item.metadata) }; }
  async realMoneyPreflight(providerId: string) { const panel = await this.providerRiskPanel(providerId); const unresolvedMismatchCount = await this.prisma.providerWalletSnapshot.count({ where: { providerId, status: { in: ['MISMATCH', 'UNKNOWN'] } } }); const blockers = [...panel.checks.filter((item) => !item.ok).map((item) => item.key)]; if (!panel.flags.transferEnabled) blockers.push('transferEnabled_false'); if (!panel.flags.walletSyncEnabled) blockers.push('walletSyncEnabled_false'); if (!panel.flags.realMoneyEnabled) blockers.push('realMoneyEnabled_false'); if (!panel.flags.webhookSettlementEnabled) blockers.push('webhookSettlementEnabled_false'); if (unresolvedMismatchCount > 0) blockers.push('unresolved_mismatch'); return { ok: blockers.length === 0, provider: panel.provider, flags: panel.flags, blockers, unresolvedMismatchCount, riskStatus: panel.status, checks: panel.checks }; }
  async reviewTransfer(id: string, actor: AdminActor, note: string) { const current = await this.prisma.gameTransfer.findUnique({ where: { id } }); if (!current) throw new NotFoundException('Game transfer not found'); const payload = this.mergeJson(current.responsePayload, { manualReview: { note, reviewedBy: actor.id, reviewedAt: new Date().toISOString() } }); const item = await this.prisma.gameTransfer.update({ where: { id }, data: { responsePayload: payload }, include: { user: { select: { id: true, username: true, phone: true } }, provider: { select: { id: true, name: true, code: true } } } }); await this.audit(actor, 'game.transfer.review', 'game_platform', id, { note }); return { ok: true, item }; }
  async reviewSnapshot(id: string, actor: AdminActor, input: { note: string; status: string }) { const current = await this.prisma.providerWalletSnapshot.findUnique({ where: { id } }); if (!current) throw new NotFoundException('Provider wallet snapshot not found'); const rawPayload = this.mergeJson(current.rawPayload, { manualReview: { note: input.note, status: input.status, reviewedBy: actor.id, reviewedAt: new Date().toISOString() } }); const mappedStatus = input.status === 'RESOLVED' ? 'MATCHED' : current.status; const item = await this.prisma.providerWalletSnapshot.update({ where: { id }, data: { rawPayload, status: mappedStatus }, include: { user: { select: { id: true, username: true, phone: true } }, provider: { select: { id: true, name: true, code: true } } } }); await this.audit(actor, input.status === 'RESOLVED' ? 'game.reconciliation.resolve' : 'game.reconciliation.review', 'game_platform', id, input); return { ok: true, item, reviewStatus: input.status }; }
  async providerRiskPanel(providerId: string) { const provider = await this.prisma.gameProvider.findUnique({ where: { id: providerId }, include: { endpoints: { orderBy: { type: 'asc' } }, credentials: { orderBy: { type: 'asc' }, select: this.credentialSelect() } } }); if (!provider) throw new NotFoundException('Game provider not found'); const [failedTransferCount, duplicateWebhookCount, latestSnapshot, unresolvedMismatchCount] = await Promise.all([this.prisma.gameTransfer.count({ where: { providerId, status: 'FAILED' } }), this.prisma.webhookLog.count({ where: { providerId, status: 'DUPLICATE' } }), this.prisma.providerWalletSnapshot.findFirst({ where: { providerId }, orderBy: { checkedAt: 'desc' } }), this.prisma.providerWalletSnapshot.count({ where: { providerId, status: { in: ['MISMATCH', 'UNKNOWN'] } } })]); const endpointSet = new Set(provider.endpoints.filter((item) => item.isEnabled).map((item) => item.type)); const credentialSet = new Set(provider.credentials.filter((item) => item.isEnabled).map((item) => item.type)); const flags = this.extractGates(provider.metadata); const checks = [{ key: 'adapter_registered', ok: this.adapterRegistry.hasAdapter(provider.code) }, { key: 'provider_active', ok: provider.status === 'ACTIVE' }, { key: 'transfer_wallet_mode', ok: provider.walletMode === 'TRANSFER' || provider.walletMode === 'HYBRID' }, { key: 'wallet_sync_enabled', ok: flags.walletSyncEnabled }, { key: 'launch_endpoint', ok: endpointSet.has('LAUNCH') }, { key: 'balance_endpoint', ok: endpointSet.has('BALANCE') }, { key: 'transfer_in_endpoint', ok: endpointSet.has('TRANSFER_IN') }, { key: 'transfer_out_endpoint', ok: endpointSet.has('TRANSFER_OUT') }, { key: 'webhook_endpoint', ok: endpointSet.has('WEBHOOK') }, { key: 'api_key', ok: credentialSet.has('API_KEY') }, { key: 'webhook_secret', ok: credentialSet.has('WEBHOOK_SECRET') }, { key: 'latest_reconciliation_safe', ok: !latestSnapshot || latestSnapshot.status === 'MATCHED' }, { key: 'no_unresolved_mismatch', ok: unresolvedMismatchCount === 0 }, { key: 'real_money_disabled_by_default', ok: !flags.realMoneyEnabled }]; const status = flags.realMoneyEnabled ? 'NEEDS_REVIEW' : checks.every((item) => item.ok) ? 'WALLET_SYNC_READY' : 'BLOCKED'; return { provider: { id: provider.id, name: provider.name, code: provider.code, status: provider.status }, flags, checks, status, failedTransferCount, duplicateWebhookCount, latestSnapshot, unresolvedMismatchCount }; }
  async listMemberSessionTransfers(sessionId: string, actor: MemberActor) { const session = await this.prisma.gameSession.findFirst({ where: { id: sessionId, userId: actor.id }, select: { id: true } }); if (!session) throw new NotFoundException('Game session not found'); const items = await this.prisma.gameTransfer.findMany({ where: { sessionId, userId: actor.id }, orderBy: { createdAt: 'desc' }, take: 30, select: { id: true, type: true, status: true, amount: true, currency: true, idempotencyKey: true, providerTransactionId: true, errorCode: true, errorMessage: true, createdAt: true, resolvedAt: true, responsePayload: true } }); return { items }; }
  async reconcileSession(sessionId: string, actor?: AdminActor) { const session = await this.prisma.gameSession.findUnique({ where: { id: sessionId }, include: { user: { select: { id: true } }, provider: { include: { endpoints: { where: { isEnabled: true }, orderBy: { type: 'asc' } }, credentials: { where: { isEnabled: true }, orderBy: { type: 'asc' }, select: this.credentialSelect() } } } } }); if (!session) throw new NotFoundException('Game session not found'); this.assertReconciliationSafetyGate(session.provider); const systemBalance = await this.expectedProviderBalanceForSession(session.id); const adapter = this.adapterRegistry.getAdapter(session.provider.code); await this.markCredentialUse(session.providerId); const result = await adapter.getBalance(this.buildAdapterContext(session.provider), { userId: session.userId, providerUserId: session.providerSessionId ?? undefined }); const providerBalance = result.ok && result.payload?.balance ? result.payload.balance : '0.00'; const difference = (Number(systemBalance) - Number(providerBalance)).toFixed(2); const status = !result.ok ? 'UNKNOWN' : Number(difference) === 0 ? 'MATCHED' : 'MISMATCH'; const snapshot = await this.prisma.providerWalletSnapshot.create({ data: { userId: session.userId, providerId: session.providerId, systemBalance, providerBalance, difference, status, rawPayload: this.safeJson({ walletSync: true, sessionId, adapterResult: result }) }, include: { user: { select: { id: true, username: true, phone: true } }, provider: { select: { id: true, name: true, code: true } } } }); const alert = status === 'MATCHED' ? null : await this.createReconciliationAlert(snapshot.id, session.userId, session.providerId, session.id, status, systemBalance, providerBalance, difference); if (actor) await this.audit(actor, 'game.reconciliation.run', 'game_platform', sessionId, { snapshotId: snapshot.id, status, difference, riskAlertId: alert?.id }); return { ok: status === 'MATCHED', snapshot, walletSync: true, riskAlert: alert }; }
  async reconcileActiveSessions(actor: AdminActor) {
    const sessions = await this.prisma.gameSession.findMany({
      where: { status: { in: ['LAUNCHED', 'ACTIVE'] } },
      orderBy: { updatedAt: 'asc' },
      take: 100,
      select: { id: true },
    });
    const results: Array<{ sessionId: string; ok: boolean; status?: string; snapshotId?: string; error?: string }> = [];
    for (const session of sessions) {
      try {
        const result = await this.reconcileSession(session.id, actor);
        results.push({ sessionId: session.id, ok: result.ok, status: result.snapshot.status, snapshotId: result.snapshot.id });
      } catch (error) {
        results.push({ sessionId: session.id, ok: false, error: error instanceof Error ? error.message : 'Reconciliation failed' });
      }
    }
    const summary = {
      scanned: results.length,
      matched: results.filter((item) => item.status === 'MATCHED').length,
      mismatch: results.filter((item) => item.status === 'MISMATCH').length,
      unknown: results.filter((item) => item.status === 'UNKNOWN').length,
      failed: results.filter((item) => item.error).length,
    };
    await this.audit(actor, 'game.reconciliation.batch_run', 'game_platform', undefined, summary);
    return { ok: summary.mismatch === 0 && summary.unknown === 0 && summary.failed === 0, summary, results };
  }
  async listSnapshots() { const items = await this.prisma.providerWalletSnapshot.findMany({ orderBy: { checkedAt: 'desc' }, take: 100, include: { user: { select: { id: true, username: true, phone: true } }, provider: { select: { id: true, name: true, code: true } } } }); return { items, summary: { total: items.length, matched: items.filter((item) => item.status === 'MATCHED').length, mismatch: items.filter((item) => item.status === 'MISMATCH').length, unknown: items.filter((item) => item.status === 'UNKNOWN').length } }; }
  async getSnapshot(id: string) { const item = await this.prisma.providerWalletSnapshot.findUnique({ where: { id }, include: { user: { select: { id: true, username: true, phone: true } }, provider: { select: { id: true, name: true, code: true } } } }); if (!item) throw new NotFoundException('Provider wallet snapshot not found'); return item; }
  async listTransfers() { const items = await this.prisma.gameTransfer.findMany({ orderBy: { createdAt: 'desc' }, take: 100, include: this.transferInclude() }); return { items, summary: { total: items.length, success: items.filter((item) => item.status === 'SUCCESS').length, failed: items.filter((item) => item.status === 'FAILED').length, pending: items.filter((item) => item.status === 'PENDING').length } }; }
  async getTransfer(id: string) { const item = await this.prisma.gameTransfer.findUnique({ where: { id }, include: this.transferInclude() }); if (!item) throw new NotFoundException('Game transfer not found'); return item; }
  async receiveWebhook(providerCode: string, headers: Record<string, string | string[] | undefined>, body: unknown, rawBody?: Buffer) {
    const payload = this.objectJson(body);
    if (!payload.eventType || typeof payload.eventType !== 'string') throw new BadRequestException('eventType is required');
    if (!payload.idempotencyKey || typeof payload.idempotencyKey !== 'string') throw new BadRequestException('idempotencyKey is required');

    const provider = await this.prisma.gameProvider.findUnique({
      where: { code: providerCode },
      include: {
        endpoints: { where: { isEnabled: true }, orderBy: { type: 'asc' } },
        credentials: { where: { isEnabled: true }, orderBy: { type: 'asc' }, select: this.credentialSelect() },
      },
    });
    if (!provider) throw new NotFoundException('Provider not found');

    const adapter = this.adapterRegistry.getAdapter(provider.code);
    await this.markCredentialUse(provider.id);
    const validation = await adapter.validateWebhook(this.buildAdapterContext(provider), headers, body, rawBody);
    const idempotencyKey = String(payload.idempotencyKey);

    if (!validation.valid) {
      const log = await this.prisma.webhookLog.create({
        data: {
          providerId: provider.id,
          eventType: String(payload.eventType),
          status: 'FAILED',
          signatureValid: false,
          idempotencyKey,
          providerTransactionId: typeof payload.providerTransactionId === 'string' ? payload.providerTransactionId : undefined,
          rawPayload: this.safeJson(payload),
          responseStatus: 400,
          errorCode: 'INVALID_SIGNATURE',
          errorMessage: validation.reason ?? 'Invalid webhook signature',
          processedAt: new Date(),
        },
      });
      return { ok: false, logId: log.id, errorCode: 'INVALID_SIGNATURE' };
    }

    const events = await adapter.parseWebhook(this.buildAdapterContext(provider), body);
    const lockKey = `${provider.id}:${idempotencyKey}`;
    return this.prisma.$transaction(async (tx) => {
      await tx.$executeRaw`SELECT pg_advisory_xact_lock(hashtext(${lockKey}))`;

      const duplicate = await tx.webhookLog.findFirst({
        where: { providerId: provider.id, idempotencyKey, status: { in: ['PROCESSED', 'DUPLICATE'] } },
      });
      if (duplicate) {
        const log = await tx.webhookLog.create({
          data: {
            providerId: provider.id,
            eventType: String(payload.eventType),
            status: 'DUPLICATE',
            signatureValid: true,
            idempotencyKey,
            providerTransactionId: typeof payload.providerTransactionId === 'string' ? payload.providerTransactionId : undefined,
            rawPayload: this.safeJson(payload),
            responseStatus: 208,
            processedAt: new Date(),
          },
        });
        return { ok: true, duplicate: true, logId: log.id, statusCode: 208 };
      }

      const log = await tx.webhookLog.create({
        data: {
          providerId: provider.id,
          eventType: String(payload.eventType),
          status: 'PROCESSED',
          signatureValid: true,
          idempotencyKey: validation.idempotencyKey ?? idempotencyKey,
          providerTransactionId: typeof payload.providerTransactionId === 'string' ? payload.providerTransactionId : events[0]?.providerTransactionId,
          rawPayload: this.safeJson(payload),
          normalizedPayload: this.safeJson({ events, walletSettlementEnabled: this.extractGates(provider.metadata).webhookSettlementEnabled }),
          responseStatus: 200,
          processedAt: new Date(),
        },
      });
      return { ok: true, logId: log.id, events, walletSettlementEnabled: this.extractGates(provider.metadata).webhookSettlementEnabled };
    });
  }

  async listWebhookLogs() { const items = await this.prisma.webhookLog.findMany({ orderBy: { createdAt: 'desc' }, take: 100, include: { provider: { select: { id: true, name: true, code: true } } } }); return { items, summary: { total: items.length, processed: items.filter((item) => item.status === 'PROCESSED').length, failed: items.filter((item) => item.status === 'FAILED').length, duplicate: items.filter((item) => item.status === 'DUPLICATE').length } }; }
  async getWebhookLog(id: string) { const item = await this.prisma.webhookLog.findUnique({ where: { id }, include: { provider: { select: { id: true, name: true, code: true } } } }); if (!item) throw new NotFoundException('Webhook log not found'); return item; }
  private assertTransferSafetyGate(provider: ProviderWithAdapterData, type: TransferKind) { if (!this.adapterRegistry.hasAdapter(provider.code)) throw new ForbiddenException('Provider adapter is not registered'); if (provider.walletMode === 'SEAMLESS') throw new ForbiddenException('Transfer endpoints are for TRANSFER/HYBRID wallet mode only'); const flags = this.extractGates(provider.metadata); if (!flags.transferEnabled) throw new ForbiddenException('transferEnabled gate is not enabled'); if (!flags.walletSyncEnabled) throw new ForbiddenException('walletSyncEnabled gate is not enabled'); const endpointSet = new Set(provider.endpoints.map((item) => item.type)); const credentialSet = new Set(provider.credentials.map((item) => item.type)); const requiredEndpoint = type === 'TRANSFER_IN' ? 'TRANSFER_IN' : 'TRANSFER_OUT'; if (!endpointSet.has(requiredEndpoint)) throw new ForbiddenException(`${requiredEndpoint} endpoint is not enabled`); if (!credentialSet.has('API_KEY')) throw new ForbiddenException('API_KEY credential is not enabled'); }
  private assertReconciliationSafetyGate(provider: ProviderWithAdapterData) { if (!this.adapterRegistry.hasAdapter(provider.code)) throw new ForbiddenException('Provider adapter is not registered'); const endpointSet = new Set(provider.endpoints.map((item) => item.type)); if (!endpointSet.has('BALANCE')) throw new ForbiddenException('BALANCE endpoint is not enabled'); }
  private buildAdapterContext(provider: ProviderWithAdapterData): ProviderAdapterContext { const endpointMap = provider.endpoints.reduce<Partial<Record<GameProviderEndpointType, string>>>((result, endpoint) => { result[endpoint.type] = endpoint.url; return result; }, {}); const credentialMap = provider.credentials.reduce<Record<string, string>>((result, credential) => { result[credential.type] = credential.encryptedValue ? this.decryptSecret(credential.encryptedValue) : credential.maskedValue; return result; }, {}); return { providerCode: provider.code, baseUrl: endpointMap.HEALTH_CHECK ?? endpointMap.LAUNCH ?? '', walletMode: provider.walletMode, currency: provider.currency, timeoutMs: Math.max(...provider.endpoints.map((endpoint) => endpoint.timeoutMs), 10000), endpointMap, credentialMap }; }
  private credentialSelect() { return { id: true, providerId: true, type: true, maskedValue: true, encryptedValue: true, isEnabled: true, rotatedAt: true, createdAt: true, updatedAt: true } as const; }
  private extractGates(metadata: unknown) { const value = this.objectJson(metadata); return { launchEnabled: value.launchEnabled !== false, transferEnabled: value.transferEnabled === true, realMoneyEnabled: value.realMoneyEnabled === true, webhookSettlementEnabled: value.webhookSettlementEnabled === true, walletSyncEnabled: value.walletSyncEnabled !== false }; }
  private transferInclude() { return { user: { select: { id: true, username: true, phone: true } }, provider: { select: { id: true, name: true, code: true } }, session: { select: { id: true, providerSessionId: true, game: { select: { id: true, name: true, providerGameCode: true } } } } } as const; }
  private async expectedProviderBalanceForSession(sessionId: string) { const items = await this.prisma.gameTransfer.findMany({ where: { sessionId, status: 'SUCCESS' }, select: { type: true, amount: true } }); const value = items.reduce((sum, item) => sum + (item.type === 'TRANSFER_IN' ? Number(item.amount) : item.type === 'TRANSFER_OUT' ? -Number(item.amount) : 0), 0); return Math.max(value, 0).toFixed(2); }
  private async createReconciliationAlert(snapshotId: string, userId: string, providerId: string, sessionId: string, status: string, systemBalance: string, providerBalance: string, difference: string) { const amount = Math.abs(Number(difference)); return this.prisma.riskAlert.create({ data: { type: 'WALLET_LEDGER_MISMATCH', severity: amount >= 10000 ? 'CRITICAL' : amount >= 1000 ? 'HIGH' : 'MEDIUM', status: 'OPEN', memberId: userId, refType: 'PROVIDER_WALLET_SNAPSHOT', refId: snapshotId, title: 'ยอด provider ไม่ตรงกับระบบ', description: `Reconciliation ${status}: system ${systemBalance}, provider ${providerBalance}, diff ${difference}`, metadata: this.safeJson({ snapshotId, providerId, sessionId, systemBalance, providerBalance, difference, source: 'game.reconciliation' }) } }); }
  private parseAmount(value: string) { const amount = Number(value); if (!Number.isFinite(amount) || amount <= 0) throw new BadRequestException('amount must be positive'); return amount; }
  private credentialKey() { const keySource = this.configService.get<string>('GAME_CREDENTIAL_SECRET') ?? this.configService.get<string>('JWT_ACCESS_KEY') ?? 'local_game_credential_key'; return createHash('sha256').update(keySource).digest(); }
  private decryptSecret(value: string) { const [, ivRaw, tagRaw, encryptedRaw] = value.split(':'); if (!ivRaw || !tagRaw || !encryptedRaw) return value; const decipher = createDecipheriv('aes-256-gcm', this.credentialKey(), Buffer.from(ivRaw, 'base64')); decipher.setAuthTag(Buffer.from(tagRaw, 'base64')); return Buffer.concat([decipher.update(Buffer.from(encryptedRaw, 'base64')), decipher.final()]).toString('utf8'); }
  private async markCredentialUse(providerId: string) { await this.prisma.gameProviderCredential.updateMany({ where: { providerId, isEnabled: true }, data: { lastUsedAt: new Date() } }); }
  private async audit(actor: AdminActor, action: string, module: string, targetId: string | undefined, newData: unknown) { await this.prisma.adminAuditLog.create({ data: buildAdminAuditData({ adminUserId: actor.id, action, module, targetId, newData }) }); }
  private safeJson(value: unknown) { return JSON.parse(JSON.stringify(value)) as Prisma.InputJsonValue; }
  private objectJson(value: unknown) { return value && typeof value === 'object' && !Array.isArray(value) ? value as Record<string, unknown> : {}; }
  private mergeJson(current: unknown, patch: Record<string, unknown>) { return this.safeJson({ ...this.objectJson(current), ...patch }); }
}
